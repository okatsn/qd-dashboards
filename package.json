{
    "name": "double-iqd-dashboard",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "observable build",
        "dev": "observable preview",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@observablehq/framework": "^1.13.0"
    }
}